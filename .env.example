# Os webhooks de booking NÃO usam env:
#  - Webhook 1 (lead → GHL/LeadConnector) e Webhook 2 (n8n) são constantes em
#    src/booking/webhook.ts (Inbound Webhook do LeadConnector é chamado direto
#    do browser; n8n é a URL fixa de produção). Ver calendar.md §3 e §6.
#  - GTM: o container é instalado no index.html (placeholder GTM_SNIPPET_HERE);
#    o código só empurra o evento `trial_booked` no dataLayer.

VITE_META_PIXEL_ID=
VITE_GTM_ID=
